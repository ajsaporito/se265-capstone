<IfModule mod_rewrite.c>
  RewriteEngine On

  # Allow index.php to access everything
  RewriteRule ^index\.php$ - [L]

  # Block direct access to PHP files
  RewriteRule \.php$ - [F,L]
  
  # Redirect all requests to index.php
  RewriteRule ^ index.php [L]
</IfModule>
